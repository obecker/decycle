plugins {
    id 'de.obqo.decycle'
}

decycle {
    including 'demo.module.**'
    ignoring 'demo.module.b.**', 'demo.module.a.**', 'c'
    slicings {
        module {
            patterns 'demo.module.(*).**'
        }
    }
}
