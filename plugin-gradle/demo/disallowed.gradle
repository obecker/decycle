plugins {
    id 'de.obqo.decycle'
}

decycle {
    including 'demo.other.**'
    slicings {
        module {
            patterns 'demo.other.(*).**'
            allowDirect 'x', 'y', 'z'
        }
    }
}
