plugins {
    id 'de.obqo.decycle'
}

decycle {
    including 'demo.module.**'
    ignoring to: 'demo.module.a.**'
    slicings {
        module {
            patterns 'demo.module.{*}.**'
        }
    }
}
